server:
  port: ${SERVER_PORT:8081}
  servlet:
    context-path: /${SERVICE_NAME:master-data}
  shutdown: graceful

spring:
  application:
    name: ${SERVICE_NAME:master-data}
  profiles:
    active: ${ENVIRONMENT:local}



